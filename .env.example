# Laravel WebAdmin origin (v `npm run dev` se `/api/platform` proxyuje sem).
VITE_PLATFORM_API_URL=http://127.0.0.1:8000

# Default web_url při vytvoření hotelu: {origin}/h/{slug}
VITE_HOSTWEB_ORIGIN=http://127.0.0.1:5173
